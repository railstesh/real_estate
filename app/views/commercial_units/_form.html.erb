<%= form_with(model: commercial_unit, local: true) do |form| %>
  <% if commercial_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commercial_unit.errors.count, "error") %> prohibited this commercial_unit from being saved:</h2>

      <ul>
      <% commercial_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :owner %>
    <%= form.text_field :owner, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :shops %>
    <%= form.number_field :shops, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :sqmt %>
    <%= form.number_field :sqmt, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :parking %>
    <%= form.number_field :parking, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, class: "form-control" %>
  </div>

  <div>
    <%= form.label :photo %>
    <%= form.file_field :photo, class: "form-control"%>
    <%= form.hidden_field :photo_cache %>

    <% if @commercial_unit.photo.present? %>
      <%= image_tag @commercial_unit.photo.thumb.url, class: "mt-4" %>
    <%end%>
  </div>

  <div class="actions">
    <%= form.submit "Submit", class: "btn btn-lg btn-success" %>
  </div>
<% end %>
